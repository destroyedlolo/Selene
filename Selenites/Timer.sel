#!./Selene

timer, err = SelTimer.create { when=3.5, interval=1, clockid=SelTimer.ClockModeConst("CLOCK_MONOTONIC") }

if err then
	print(err)
	return
end

Selene.Sleep(.75);

print( timer:Get() )

print("Timer reseted")
timer:Reset()
print( timer:Get() )

timer:Release()

