#!./Selene --dfb:quiet 

Brk, err = SelMQTT.connect( "tcp://localhost:1883", { reliable=false  } )
if not Brk then
	print( err )
	return
end

_, err = Brk:subscribe( { { topic = "tata" }, {topic = 1}, { topic = "toto" } } )
if err then
	print( err )
	return
end

