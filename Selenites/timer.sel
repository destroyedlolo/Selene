#!./Selene --dfb:quiet 

timer, err = SelTimer.create( 3.5, 1, SelTimer.ClockModeConst("CLOCK_MONOTONIC") )

if err then
	print(err)
	return
end

Selene.Sleep(1);

print( timer:Get() )

timer:Release()

